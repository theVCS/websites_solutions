#include <bits/stdc++.h>
using namespace std;
int main(int argc, char const *argv[]){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    
    int w;
    cin>>w;
    if (w % 2 || w == 2)
    {
        cout<<"NO";
    }
    else
    {
        cout<<"YES";
    }
    

    return 0;
}